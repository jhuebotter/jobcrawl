import React from 'react';

const AddEntityForm = () => {
    return (
        <div>
            <h2>Add New Entity</h2>
            {/* Placeholder for the add entity form */}
        </div>
    );
};

export default AddEntityForm;
